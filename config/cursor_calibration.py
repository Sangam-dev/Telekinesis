"""Calibration settings for mapping camera hand movement to the screen."""

# The active camera zone is stretched across the full screen. A narrower zone
# reduces the arm movement needed to move the pointer across the display.
ACTIVE_ZONE = (0.22, 0.78, 0.20, 0.80)
